spring.application.name=PokerPulse

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/pokerpulse_db}

spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Rzymek13!}

spring.sql.init.mode=always

# Logging (optional)
logging.level.org.springframework.jdbc.core=DEBUG
